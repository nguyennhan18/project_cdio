Dataset ảnh KHÔNG được push lên GitHub vì quá nặng.

Cách tải dataset:
  Xem hướng dẫn trong README.md — tải từ Roboflow bằng API key.

Cấu trúc thư mục sau khi tải:
  dataset/
    images/
      train/   ← ảnh train (.jpg)
      val/     ← ảnh val (.jpg)
    labels/
      train/   ← nhãn YOLO (.txt)
      val/     ← nhãn YOLO (.txt)
